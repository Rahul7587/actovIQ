server:
  port: 8080

spring:
  application:
    name: actoviq-api

app:
  cors:
    allowed-origins: http://localhost:5173,http://127.0.0.1:5173

management:
  endpoints:
    web:
      exposure:
        include: health,info
